@model ContosoUniversity.Models.Entities.Committee

@{
    ViewData["Title"] = "Delete";
}

<h2>Archive</h2>

<h3>Are you sure you want to archive this committie? <br />This action cannot be cancelled</h3>
<div>
    <h4>Committee</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Chair)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Chair.FullName)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Level)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Level)
        </dd>
    </dl>
    
    <form asp-action="ConfirmArchive">
        <div class="form-actions no-color btn-group">
            <input type="hidden" asp-for="CommitteeID" />
            <input type="submit" value="Archive" class="btn btn-warning" />
            <a class="btn btn-info" asp-action="Index">Back to List</a>
        </div>
    </form>
</div>
